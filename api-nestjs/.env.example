PORT=3002

DB_HOST=localhost
DB_NAME=defis_photo
DB_USER=defis_photo
DB_PASS=

# Long random string — never reuse across projects (and never reuse the one from server/).
JWT_SECRET=change-me
JWT_EXPIRES_IN=30d

# Comma-separated list of origins allowed to call this API.
CORS_ORIGINS=http://localhost:5173,https://defis-photo.vercel.app
