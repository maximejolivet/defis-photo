import type { CapacitorConfig } from '@capacitor/cli';

// appId : identifiant définitif de l'app sur les stores, à ne plus changer une fois publiée.
const config: CapacitorConfig = {
  appId: 'fr.jolivetmaxime.defisphoto',
  appName: 'Défis photo',
  webDir: 'dist',
};

export default config;
